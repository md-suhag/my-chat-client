import { createTheme } from "@mui/material/styles";

const customTheme = createTheme({
  palette: {
    primary: {
      main: "#2694ab",
    },
    secondary: {
      main: "#dc004e",
    },
    mode: "light",
  },
});

export default customTheme;
